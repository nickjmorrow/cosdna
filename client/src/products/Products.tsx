import * as React from 'react';
import { Typography } from '~/core/Typography';

export const Products: React.FC = () => {
    return (
        <div>
            <Typography fontFamilyVariant={'title'} typographyVariant={'h2'}>
                Testing 1
            </Typography>
        </div>
    );
};
